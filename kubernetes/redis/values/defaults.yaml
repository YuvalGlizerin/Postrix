architecture: standalone

master:
  persistence:
    enabled: true 
    size: 1Gi
    storageClass: gp2
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "150m"
      memory: "192Mi"

auth:
  enabled: false

replica:
  replicaCount: 0
