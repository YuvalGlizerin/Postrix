{
  "name": "creatomate",
  "version": "1.0.0",
  "type": "module",
  "description": "A package that will contain creatomate api helper functions",
  "main": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "jest --config ../../jest.config.mjs --rootDir ./"
  },
  "devDependencies": {
    "node-fetch": "^3.3.2"
  },
  "dependencies": {
    "logger": "file:../logger"
  }
}
