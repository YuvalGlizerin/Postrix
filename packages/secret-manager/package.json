{
  "name": "secret-manager",
  "version": "1.0.0",
  "type": "module",
  "description": "Initialize all envs/secrets specified in the .env file into process.env",
  "main": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "jest --config ../../jest.config.mjs --rootDir ./"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.799.0",
    "@aws-sdk/credential-providers": "^3.799.0"
  }
}
