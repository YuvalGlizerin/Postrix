{
  "name": "secret-manager",
  "version": "1.0.0",
  "type": "module",
  "description": "Initialize all secrets specified in the .env file",
  "main": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "jest --config ../../jest.config.mjs --rootDir ./"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.799.0",
    "@aws-sdk/credential-providers": "^3.799.0",
    "@kubernetes/client-node": "^1.3.0",
    "utils": "*"
  }
}
