{
  "name": "whatsapp-utils",
  "version": "1.0.0",
  "type": "module",
  "description": "A package that will contain whatsapp api helper functions",
  "main": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "jest --config ../../jest.config.mjs --rootDir ./"
  },
  "dependencies": {
    "file-system": "file:../file-system",
    "logger": "file:../logger"
  },
  "devDependencies": {
    "@types/express": "^5.0.1"
  }
}
