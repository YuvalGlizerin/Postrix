{
  "name": "file-system",
  "version": "1.0.0",
  "type": "module",
  "description": "A package that will contain file system helper functions",
  "main": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "jest --config ../../jest.config.mjs --rootDir ./"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.802.0",
    "@aws-sdk/credential-providers": "^3.799.0",
    "logger": "file:../logger"
  }
}
