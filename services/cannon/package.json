{
  "name": "cannon",
  "version": "1.0.0",
  "type": "module",
  "description": "Cannon core service",
  "main": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "start:local": "ENV=local node --experimental-strip-types --watch src/index.ts",
    "start:server": "node --experimental-strip-types src/index.ts",
    "test": "jest --config ../../jest.config.mjs --rootDir ./"
  },
  "dependencies": {
    "cannon-db": "file:../../packages/cannon-db",
    "env-loader": "file:../../packages/env-loader",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/pg": "^8.11.11"
  }
}
