# Postrix

# Installation
[Github Actions] - https://github.com/nektos/act, save `act` to path.

# PNPM
Install a package(Install local redis package to logger)
```
pnpm add logger --filter redis --workspace
```

Remove a package(Remove redis package from logger)
```
pnpm remove logger --filter redis
```
